<h1>Editing Institution_person</h1>

<% form_for(@institution_person) do |f| %>
  <%= f.error_messages %>

<fieldset>
    <legend>Institution_person</legend>
    <%= render :partial => "form", :locals=>{ :f => f} %>
</fieldset>

  <fieldset class="button_holder">
    <%= f.submit "Update" %>
  </fieldset>
<% end %>

<div class="bottom_nav">
<%= link_to 'Show', @institution_person %> |
<%= link_to 'Back', institution_people_path %>
</div>